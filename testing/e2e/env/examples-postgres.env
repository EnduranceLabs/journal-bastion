# Drives the SHIPPED examples/bastion.json against the Dockerized Postgres.
# The remote-api entry in bastion.json points at a non-existent host and is
# expected to fail and be skipped (resilient startup) — REMOTE_MCP_AUTHORIZATION
# is a dummy so config resolution passes.
JOURNAL_BASTION_TOKEN=gw_demo
JOURNAL_BASTION_URL=ws://127.0.0.1:8080

POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5433
POSTGRES_DATABASE=analytics
POSTGRES_USER=journal_bastion_ro
POSTGRES_PASSWORD=ro_pw

REMOTE_MCP_AUTHORIZATION=Bearer dummy-not-used
